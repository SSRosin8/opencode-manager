# opencode-manager 项目规范

本文件适用于整个仓库。任何人工或自动化修改都必须遵守；若子目录存在更具体的 `AGENTS.md`，以更具体规则为补充，但不得降低本文件的安全和质量要求。

## 目标与原则

- 保持 OpenAI 兼容接口、OpenCode Zen 转发、Worker 调度、代理隔离和管理后台之间的职责边界。
- 优先做小而可验证的修改。重构不得无意改变 HTTP 状态码、响应结构、调度顺序、持久化格式或安全边界。
- 复用现有模块和类型；只有在确实降低耦合、重复或测试成本时才新增抽象。
- 代码、测试和文档必须同步。用户可见行为变化必须更新 README 或 `docs/`。

## 文件与函数规模

- `src/`、`tests/`、`scripts/` 下所有手写代码文件硬上限为 **1000 行**，目标不超过 **600 行**。
- 接近 600 行时应按领域职责规划拆分，不得通过压缩格式、合并语句或删除可读性内容规避限制。
- 单个函数建议不超过 **80 行**，硬上限为 **200 行**。超过建议值时应检查是否混合了解析、校验、业务编排、I/O 或响应格式化；超过硬上限必须拆分。
- 单个模块应有一个清晰的主要职责。不得用 `utils.ts`、`helpers.ts` 等无领域含义的大杂烩文件承接拆分代码。
- 生成文件、依赖、锁文件和纯文档不受手写代码行数限制；生成产物不得提交到源码目录伪装成手写模块。
- `npm run check:structure` 是行数硬限制的自动校验入口。新增语言或代码扩展名时必须同步扩展该脚本。

## 模块职责

- `src/index.ts`：进程入口、启动日志和优雅退出，不承载业务逻辑。
- `src/server/`：HTTP 装配、路由和协议适配。路由只负责解析输入、调用领域服务和映射响应。
- `src/server/routes/`：按 `relay`、`workers`、`proxyPool`、`subscriptions` 等领域拆分路由，禁止形成新的单文件路由总表。
- `src/server/admin/`：管理页面模板、样式、脚本和静态资源。视图渲染、状态管理和 API 调用应分离。
- `src/relay/`：OpenAI/OpenCode 请求转换、请求头、URL 与账号调度，不依赖管理 UI。
- `src/proxy/`：代理解析、出口解析、Clash/Mihomo 桥接、探测和上游发送；协议解析不得进入 HTTP 路由。
- `src/settings/`：配置归一化、持久化、运行时状态和用量统计；不得依赖 HTTP 请求/响应对象。
- `tests/`：目录和命名应映射被测领域。大型集成测试按 API 领域拆分，共享搭建代码放到具名测试支持模块。
- 跨层依赖方向为 `index -> server -> domain/settings`。领域模块不得反向导入 `server` 或管理页面模块。
- 对外共享类型放在其拥有者模块中；不要建立与实现脱离的全局类型仓库。

目录职责和依赖方向详见 `docs/ARCHITECTURE.zh-CN.md`。

## TypeScript 与实现约定

- 保持 ESM 和 `NodeNext` 约定；本地 TypeScript 导入继续使用编译后的 `.js` 后缀。
- 保持 `strict` 通过，不使用 `any`、`@ts-ignore` 或无说明的类型断言逃避建模。
- 结构化输入使用类型守卫、标准解析器或已有归一化函数，不以脆弱字符串处理代替。
- 网络、文件和定时器资源必须有明确生命周期；新增外部请求必须设置可配置或有界超时。
- 错误响应保持稳定、可诊断，但不得包含 API Key、令牌、代理口令、订阅凭证或完整敏感 URL。
- 注释解释约束和原因，不复述代码。新增 UI 文案必须同时维护中英文。

## 安全与敏感数据

- `data/`、`.env*`、真实 Zen Key、Relay Token、Clash Secret、代理口令和订阅 URL 永远不得提交。
- 测试只能使用明显虚构的 Key、IP、域名和订阅内容，不得复制真实配置作为 fixture。
- 日志、错误、统计和测试快照必须脱敏；修改凭证相关路径时必须增加泄漏回归测试。
- Relay Token 保护所有模型转发别名；新增模型接口时必须加入同一鉴权边界。
- 管理 API 当前没有独立认证。除非完整实现认证，不得把默认监听地址改离 `127.0.0.1`，也不得在文档中暗示可直接暴露公网。
- 请求体、远端响应、订阅内容和批处理输入应有大小或数量上限；不得引入无界内存累积。
- 代理配置无效时必须明确失败，禁止静默直连导致出口隔离失效。
- 不执行真实批测、真实上游调用或读取本机 `data/` 来完成自动化测试。

## 测试与验证

- 纯函数和归一化逻辑使用单元测试；路由、流式转发、持久化和调度故障转移使用集成测试。
- 每个缺陷修复必须有能在修复前失败的回归测试。重构必须保持或提高对应领域覆盖。
- 测试必须隔离网络、端口、时间和文件系统；使用临时目录、注入 fetch 和虚构响应，测试结束必须释放服务器和定时器。
- 避免依赖固定 sleep 的脆弱测试，优先轮询可观察状态并设置短超时。
- 提交前至少运行：

```bash
npm run validate
git diff --check
```

- UI 结构或交互变化还需在桌面和移动视口做浏览器检查；不得用真实节点触发批量测试。

## Git 与变更范围

- 开始和提交前检查 `git status`。工作区可能包含用户修改，不覆盖、不回滚、不顺带提交。
- 提交应按一个清晰目的组织；文档、测试和实现属于同一行为变更时应一起提交。
- 不提交 `dist/`、`node_modules/`、`data/`、日志、临时截图或编辑器文件。
- `package.json` 与 `package-lock.json` 的依赖变更必须成对且有明确原因；不要把无关 lockfile 漂移混入提交。
- 禁止使用破坏性 Git 命令清理用户工作区。推送前检查已暂存 diff 和敏感信息。
- Commit message 使用简洁的 Conventional Commits 风格，例如 `refactor: split admin routes`。

## 完成定义

- 代码满足文件和函数规模、模块职责与依赖方向规则。
- 新增/修改行为有对应测试，`npm run validate` 与 `git diff --check` 通过。
- README、使用指南或架构文档已同步用户可见变化。
- 已检查暂存内容没有真实凭证、运行数据、无关 lockfile 变化和生成产物。
